import styled from 'styled-components';

export const Button = styled.button`
  width: 95%;
  padding: 16px;
  border: none;
  margin: 10px auto;
  border-radius: 4px;
  height: 56px;
  cursor: pointer;
  outline: none;
  background: linear-gradient(
    90deg,
    rgba(5, 76, 181, 1) 0%,
    rgba(43, 117, 227, 1) 100%
  );
  color: #fff;
  text-transform: capitalize;
`;
